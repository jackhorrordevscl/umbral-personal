# Copy to .env. Optional in dev: without it the client falls back to localhost
# (see src/api/client.ts). Required for builds served from another host/LAN IP.
VITE_API_URL=http://localhost:3001/api/v1